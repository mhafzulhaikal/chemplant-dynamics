# Git
.git
.gitignore
.python-version

# Python bytecode
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments and env files (never bake secrets into the image)
.env
.env.example
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# NiceGUI cache
.nicegui/

# UV backup
uv.lock.bak

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo

# Docker files (not needed inside the image)
Dockerfile
docker-compose.yml
.dockerignore

# Deployment and docs
deploy/
README.md
nginx.conf

# Test / scratch data (keep cases/ if runtime data is needed; remove if not)
# cases/
